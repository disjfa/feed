{% extends 'base.html.twig' %}

{% block content %}
    <div class="container py-3">
        <div class="row justify-content-around">
            <div class="col-lg-4">
                <h1>Login</h1>
                <a href="{{ path('connect_facebook_start') }}" class="btn btn-outline-primary btn-block">
                    <i class="fab fa-fw fa-facebook-f"></i>
                    Facebook
                </a>
            </div>
        </div>
    </div>
{% endblock %}
